{
  "answer_format": "integer",
  "input": "def sum_to_n(n):\n    total = 0\n    for i in range(1, n):\n        total += i\n    return total\n",
  "round_id": 60,
  "statement": "The input is a Python 3 function that is meant to return the sum of all integers from 1 to n inclusive. It contains exactly one bug: an off-by-one, a wrong operator, or a wrong initial value. Fix the bug, then evaluate the corrected function for the call `sum_to_n(45)`. Answer with the integer it returns.",
  "title": "Blue belt: fix sum_to_n"
}